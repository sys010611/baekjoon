#include <iostream>
#include <queue>
#include <vector>
#include <cstring>
#include <queue>
#include <climits>
#include <numeric>
using namespace std;

signed main()
{

	for(int t=1;;t++)
	{
		int n;
		cin >> n;
		if (n == 0)return 0;

		while (n--)
		{
			int input;
			cin >> input;
		}
		printf("Case %d: Sorting... done!\n", t);
	}
}
