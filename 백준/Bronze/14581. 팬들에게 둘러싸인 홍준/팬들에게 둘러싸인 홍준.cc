#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
#include <climits>
using namespace std;


signed main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);

    string s; cin >> s;

    cout << ":fan::fan::fan:" << endl;
    cout << ":fan::"<< s << "::fan:" << endl;
    cout << ":fan::fan::fan:";
}
