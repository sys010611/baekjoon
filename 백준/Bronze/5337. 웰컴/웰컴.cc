#include <iostream>
#include <cstring>
#include <algorithm>
#include <vector>
#include <bitset>
#include <queue>
#include <climits>
#include <cmath>
#include <stack>
using namespace std;

int dp[5002]; // n번째 자리까지 고려했을 때 가능한 경우의 수
#define DIV 1000000

signed main()
{
	ios::sync_with_stdio(false);
	cin.tie(NULL);

    string s = R"(.  .   .
|  | _ | _. _ ._ _  _
|/\|(/.|(_.(_)[ | )(/.)";
    cout << s;
}
