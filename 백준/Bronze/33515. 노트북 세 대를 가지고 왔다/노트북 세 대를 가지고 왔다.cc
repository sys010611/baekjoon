#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
#include <climits>
#include <map>
using namespace std;

map<int, int> m;

int solution()
{

}

signed main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);

    int a, b; cin >> a >> b; cout << min(a, b);
}
